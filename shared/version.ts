// Single source of truth for the app version.
// Follows semantic versioning: MAJOR.MINOR.PATCH
//
// MAJOR — fundamental changes (e.g. new game modes, multiplayer launch, major redesign)
// MINOR — new features (e.g. AI opponent, auth, leaderboards, new screen)
// PATCH — bug fixes, polish, small tweaks (e.g. viewport fix, style adjustments)
//
// Bump this value here; it propagates to all UI automatically.
export const APP_VERSION = '0.10.1';
